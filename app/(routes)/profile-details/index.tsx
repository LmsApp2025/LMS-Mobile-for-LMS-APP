// This is the new entry point for the route
import ProfileDetailScreen from "@/screens/profile/profile.details.screen";

export default function ProfileDetailsPage() {
  return <ProfileDetailScreen />;
}